#include <bits/stdc++.h>
#define ll long long int
using namespace std;
void solve()
{
    int n, m, d;
    cin >> n >> m >> d;
    vector<int> v(m);
    for (auto &x : v)
        cin >> x;
        
}
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int t = 1;
    cin >> t;
    for (int cs = 1; cs <= t; cs++)
    {
        // cout << "Case #" << cs << ": ";
        solve();
    }
    return 0;
}